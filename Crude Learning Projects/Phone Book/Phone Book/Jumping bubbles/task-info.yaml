type: edu
custom_name: stage2
files:
- name: src/phonebook/Main.kt
  visible: true
  text: |
    package phonebook

    fun main() {
        println("Hello, World!")
    }
  learner_created: false
- name: test/PhoneBookTest.java
  visible: false
  text: |
    import org.hyperskill.hstest.stage.StageTest;
    import org.hyperskill.hstest.testcase.CheckResult;
    import org.hyperskill.hstest.testcase.TestCase;
    import phonebook.MainKt;

    import java.util.Arrays;
    import java.util.List;

    public class PhoneBookTest extends StageTest {
        public PhoneBookTest() {
            super(MainKt.class);
        }

        @Override
        public List<TestCase> generate() {
            return Arrays.asList(
                new TestCase().setTimeLimit(30 * 60 * 1000)
            );
        }

        @Override
        public CheckResult check(String reply, Object clue) {
            reply = reply.toLowerCase();
            return new CheckResult(
                reply.contains("start searching")
                    && reply.contains("found")
                    && reply.contains("min.")
                    && reply.contains("sec.")
                    && reply.contains("ms.")
                    && reply.contains("sorting time")
                    && reply.contains("searching time")
                    && reply.contains("linear search")
                    && reply.contains("bubble sort")
                    && reply.contains("jump search"));
        }
    }
  learner_created: false
- name: src/phonebook/User.kt
  visible: true
  learner_created: true
feedback_link: https://hyperskill.org/projects/86/stages/477/implement
status: Failed
feedback:
  message: |-
    Wrong answer in test #1

    Please find below the output of your program during this failed test.

    ---



    Telephone Directory Bubble Sort
    Instructed to sort our telephone directory by alphabetical name
    firstly convert to a list of entries then sort the list

    Time to created unsorted list of users is 00 min. 00 sec. 843 ms.

    Printing 5 sample unsorted entries
    Jan Atiana 60478194
    Amalia Gaelan 75554798
    Jobie Elder 23157053
    Cicely Coughlin 40008938
    Lil Tudela 7713408
  time: Mon, 26 Oct 2020 07:56:22 UTC
record: -1
